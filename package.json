{
    "scripts": {
        "htpasswd": "docker exec -it nginx \"./htpasswd.sh\" && docker cp \"nginx:/etc/nginx/.htpasswd\" \"./.htpasswd\"",
        "dev": "pnpm \"/dev:.*/\"",
        "dev:server": "docker compose up",
        "dev:css": "pnpm tailwindcss -i \"./src/styles/global.css\" -o \"./assets/global.css\" -w",
        "dev:minify": "pnpm node esbuild.config.js",
        "format": "pnpm \"/format:.*/\"",
        "format:prettier": "prettier --write .",
        "format:sort": "pnpm dlx tailwind-sorter sort -i \"./src/**/*.php\"",
        "postinstall": "pnpm dlx shx cp \"./.env.example\" \"./.env\""
    },
    "type": "module",
    "devDependencies": {
        "@prettier/plugin-php": "^0.24.0",
        "@tailwindcss/cli": "^4.1.12",
        "@types/node": "^24.3.0",
        "array-iter": "^1.0.0",
        "esbuild": "^0.25.9",
        "prettier": "^3.6.2",
        "tailwindcss": "^4.1.12",
        "tinyglobby": "^0.2.14"
    }
}
